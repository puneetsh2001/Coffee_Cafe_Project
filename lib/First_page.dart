import 'package:flutter/material.dart';

class Firstpage extends StatelessWidget{
  Widget build(BuildContext context){
    return
        Scaffold(
          appBar: AppBar(
            title: Text("First_Page", style: TextStyle(fontSize: 50, backgroundColor: Colors.pink))
          )
        );

  }
}